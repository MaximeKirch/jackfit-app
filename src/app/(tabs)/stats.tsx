import { View, Text } from 'react-native'

export default function StatsScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>JackFit — Stats</Text>
    </View>
  )
}
